# PR433 NVIDIA Platform Chat Synthetic Proof

Run:

`docs/roadmap/PR433_NVIDIA_PLATFORM_CHAT_SYNTHETIC_PROOF_DAEDALUS.md`

Goal: prove the NVIDIA platform chat path for staging/dev using synthetic-only
public-safe prompt content, or return the exact blocker.

No private archive text, Memory, Continuity, replay corpus anchors, real user
prompts, embeddings switch, Cloudflare, Redis, workers, queues, or broad model
gateway work.

Wake ARGUS with proof/patch result if complete. Wake MIMIR with the exact
blocker if config/quota/model/data-policy/provider support is missing.
