# PR528C8 - Public Partner Corpus Hosted Review

Owner: ARGUS / A3

Opened by: MIMIR / A1

Date opened: 2026-07-16

Status: Open - independent hosted deployment and retained-state review

## Review Target

Independently review
`PR528B11_ACCEPTED_SEARCH_DEPLOY_AND_PUBLIC_CORPUS_COMPLETION_DAEDALUS_RESULT.md`
against PR528B11, PR528B9, PR528B7, the accepted PR528B2 public packet, and the
accepted source/review chain through `6794ac99`.

The first operator attempt was fully cleaned after a browser-selector evidence
failure; a second fresh run is retained. Do not accept narrative continuity.
Bind current hosted facts, protected ledgers/receipts, exact row relationships,
and independent route behavior.

## Deployment Review

1. Prove API/web readiness and actual deployment identity. Bind exact accepted
   ancestry and production hashes for `documents.ts` and `discover.ts`; require
   the accepted discussion and search behavior with no intervening runtime
   source drift.
2. Require migration range `025-086` and all seven migration proofs green. No
   new migration is expected.

## Retained Owner And Data Review

3. Decrypt identifiers only in-process. Prove exactly one retained tagged Auth
   owner/profile with public identity `Station Guide` / `station-guide`,
   nonbilling `creator` tier, inactive subscription, null Stripe identifiers,
   admin false, zero token/top-up/storage use, and no leaked private Auth
   metadata.
4. Prove exactly one public Continuity Field Notes Space and its four normal
   default pages, one approved summarized document, exactly two prior version
   snapshots with current version `3`, and exactly one linked discussion.
5. Audit version truth: draft/comments-off/no publication/no pointer; published
   comments-off/no pointer; current comments-on/one pointer. Require genuine
   operation timestamps and no fabricated/backdated state.
6. Require the current document's exact approved title, slug, summary, body,
   public visibility, Space link, and `user_authored` / `manual` provenance.
7. Require the one thread's exact approved title/body, public active visible
   normal moderation, owner authorship, documents-and-codexes category, same
   Space/document links, zero engagement, and the same id through document
   pointer/discussion/thread readback. Require zero second/default `Discuss:`
   thread.

## Independent Product Review

8. Re-run all seven anonymous surfaces: Latest Discover, public Space, public
   document, discussion link, thread detail, exact title search, and distinctive
   canonical-body phrase search. Both searches must return the same document
   exactly once. Require summary-based Discover/Space excerpts and separate
   summary/body document rendering.
9. Follow the visible public browser chain independently at desktop and a
   representative mobile viewport. This is a functional route/readback check,
   not the final ARIADNE human-eye partner rehearsal.
10. Prove the linked discussion remains deduplicated from the standalone public
    feed and that no explicit `discover_feed` promotion row exists.

## Privacy, Cleanup, And Forbidden Scope

11. Independently prove the cleaned first attempt left no second owner, profile,
    Space, page, document, version, thread, token/storage, community-profile,
    Auth-session, storage-object, tagged, or orphan residue beyond the exact
    retained run.
12. Re-run all available public-owner forbidden-scope checks and require zero
    rows. Classify unavailable connector scopes explicitly; do not treat them as
    checked.
13. Re-run the private Aster verifier before and after review. Require exact
    three fractional weights, invariant hashes, 1145-byte storage/accounting,
    zero private disclosure, zero forbidden rows, and zero chat/provider/token
    use or mutation.

## Mutation Boundary

Read-only except for ordinary fresh owner sign-in/session effects required for
independent owner-path checks; revoke only those exact review sessions. Do not
edit the public corpus, deploy, create engagement, configure providers, call
chat, alter private Aster, or clean retained review content.

Commit only public-safe aggregate evidence. Never print or commit credentials,
tokens, owner/row ids, protected ledger contents, private metadata, or private
timestamps.

## Result And Handoff

Create:

`docs/roadmap/PR528C8_PUBLIC_PARTNER_CORPUS_HOSTED_REVIEW_ARGUS_RESULT.md`

Use one exact verdict:

```text
ACCEPT_PR528B11_PUBLIC_PARTNER_CORPUS_FOR_ARIADNE
BLOCK_PR528B11_PUBLIC_PARTNER_CORPUS_<EXACT_REASON>
```

Commit and push public-safe evidence, then wake MIMIR:

```text
WAKEUP A1:
Codename: MIMIR
Summary:
- ARGUS independently reviewed the accepted deployment and retained Station Guide public partner corpus.
Verdict:
- ACCEPT_PR528B11_PUBLIC_PARTNER_CORPUS_FOR_ARIADNE (or exact blocker)
Task:
- If accepted, route ARIADNE's exact-SHA human partner rehearsal before PR528 closeout.
```
