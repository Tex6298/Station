# PR528B9 - Accepted API Deploy And Public Corpus Retry

Owner: DAEDALUS / A2

Opened by: MIMIR / A1

Date opened: 2026-07-16

Status: Open - serialized hosted deployment and bounded product-data operation

## Purpose

Deploy the PR528B8 document-discussion repair accepted by ARGUS, then create
and verify the exact retained public Station Guide partner chain through normal
product APIs. This lane supersedes only the contradictory discussion sequence
in PR528B7; every other public copy, ownership, privacy, retention, write, and
cleanup boundary remains authoritative.

## Accepted Source

The accepted code commit is:

```text
f8349f09433bdee9dc8770a2623829cc8f03beb3
```

ARGUS verdict:

```text
ACCEPT_PR528B8_DOCUMENT_DISCUSSION_OWNER_CUSTOMIZATION_FOR_DEPLOYMENT
```

The deployment may report a later docs-only coordination commit only if the
runtime API source diff from `f8349f09` is empty. Bind and record that fact
before hosted product writes. No migration is required.

## Phase 1 - Serialized API Deployment

1. Require a clean source boundary apart from ignored agent state. Bind the
   accepted commit, current `fork/main`, API source-tree diff, current Railway
   API/web deployment identities, migration readiness, and public/private
   corpus baselines without printing secrets.
2. Wait for or trigger only the required Railway API deployment. Do not deploy
   an unrelated service merely for activity. If Railway automatically rebuilds
   web from the same commit, verify it remains ready and source-compatible.
3. Require the API deployment to contain the exact accepted runtime source,
   `/health/deployment` to be ready, migration range to remain `025-086`, and
   all seven migration proofs to remain green.
4. Re-run the accepted focused local suites if any checkout or source ambiguity
   exists. Do not proceed to corpus writes while deployment identity or
   readiness is ambiguous.

## Phase 2 - Revised PR528B7 Product Sequence

Before the first write, repeat all collision, forbidden-count, private Aster,
and encrypted cleanup-ledger gates from PR528B7 and the accepted PR528B2 public
packet. Require zero collision for `station-guide`, `continuity-field-notes`,
the approved document slug/title, and approved thread title.

Then execute exactly:

1. Create the dedicated `Station Guide` / `station-guide` review owner through
   normal signup. Add only the private fixture purpose/role/cleanup metadata,
   set only the nonbilling `creator` entitlement through the controlled
   service-role operation, keep subscription inactive, Stripe fields null and
   admin false, then sign in again for current entitlement readback.
2. Through owner product APIs, create the exact public Continuity Field Notes
   Space from the accepted content packet.
3. Create the exact summarized document as a draft with comments disabled,
   then publish it while comments remain disabled. Preserve summary separately
   from the unchanged canonical body.
4. Enable comments through the existing owner document PATCH. Capture the one
   helper-created linked discussion id returned by that operation. Require
   exactly one linked thread and the expected documents-and-codexes category.
5. Call `PATCH /documents/:id/discussion` as the owner with the exact accepted
   thread title and body. Require the response to retain the same thread id.
6. Prove no second linked thread exists and no separate default `Discuss: ...`
   row remains. The helper-created row must now carry the exact approved copy.
7. Validate anonymously through Latest Discover, exact-title and body-phrase
   search, public Space, public document, document discussion, and thread
   detail. Follow the route as a human chain as well as direct API readback.
8. Prove first-class summary rendering and summary-based Discover/Space
   excerpts, exact user-authored/manual provenance, truthful visibility and
   discussion labels, genuine server timestamps, and zero fabricated
   engagement.

Do not call `POST /forums/threads` for this document. Do not use a direct
database thread insert, temporary comments toggle, duplicate/tombstone bridge,
or service-role product-data write.

## Retained Write Boundary

The retained public result remains exactly the PR528B7 allow-list:

- one Auth/profile review owner plus trigger-maintained zero token/storage
  rows;
- one Space and its four standard Space pages;
- one document and normal version rows from create/publish/comment-enable;
- one linked thread and at most one normal community profile.

Do not write a public persona, `discover_feed`, votes, comments, Watches,
witnesses, reports, moderation, pin/featured/staff state, notifications,
billing, top-ups, subscription, provider/BYOK, chat, traces, token
transactions, Developer Spaces, exports, connectors, queues, or Cloudflare.

The retained private Aster owner/corpus is read-only. Reprove its exact three
fractional Memory weights, invariant hashes, private storage/accounting,
forbidden-scope counts, and zero public/cross-owner disclosure before and after
the public operation.

## Failure And Cleanup

- If deployment acceptance fails, make no hosted product/Auth write.
- If any public write begins but the exact retained boundary cannot be reached
  and proved, use the encrypted ledger to remove the exact public document,
  linked discussion, Space, sessions, Auth owner, and all owner-scoped residue;
  then prove the target collisions and forbidden counts returned to zero.
- Never clean, alter, or re-embed the retained private Aster corpus.
- Never print or commit credentials, tokens, owner/row ids, private metadata,
  private timestamps, or ledger contents.

## Validation And Evidence

Commit only public-safe aggregate evidence. Bind at minimum:

- accepted source and actual deployment identity/source equivalence;
- health/migration readiness;
- exact retained row cardinalities and one-thread identity;
- exact public route/search/readback outcomes;
- summary/body separation and provenance;
- private-corpus invariant and disclosure checks;
- forbidden-scope zero counts; and
- encrypted cleanup coverage.

## Result And Handoff

Create:

`docs/roadmap/PR528B9_ACCEPTED_API_DEPLOY_AND_PUBLIC_CORPUS_RETRY_DAEDALUS_RESULT.md`

Use one exact verdict:

```text
READY_PR528B9_ACCEPTED_API_DEPLOY_AND_PUBLIC_CORPUS_FOR_ARGUS
BLOCK_PR528B9_<EXACT_REASON>_WITH_<ZERO_OR_EXACT_RETAINED_STATE>
```

Commit and push public-safe evidence only, then wake MIMIR:

```text
WAKEUP A1:
Codename: MIMIR
Summary:
- DAEDALUS deployed the accepted document-discussion source and ran the revised bounded public Station Guide corpus operation.
Verdict:
- READY_PR528B9_ACCEPTED_API_DEPLOY_AND_PUBLIC_CORPUS_FOR_ARGUS (or exact blocker/state)
Task:
- Route ARGUS independent deployment/corpus review before ARIADNE human rehearsal.
```
