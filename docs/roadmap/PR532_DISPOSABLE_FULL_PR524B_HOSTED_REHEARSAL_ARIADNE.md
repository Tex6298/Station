# PR532 Disposable Full PR524B Hosted Rehearsal

Date: 2026-07-18

Owner: ARIADNE / A4

Review target: ARGUS / A3

Status:

```text
READY_PR532_FOR_ARIADNE_HOSTED_REHEARSAL
```

## Authority

PR532A is applied, ledgered, reconciled, and closed. The fresh PR532 read-only
operator preflight has zero unsafe cleanup guards and zero product mutation.

Sources:

- `docs/roadmap/PR532_DISPOSABLE_FULL_PR524B_HOSTED_PROOF_MIMIR.md`
- `docs/roadmap/PR532A_APPEND_ONLY_PARENT_CASCADE_CLEANUP_HOSTED_RESULT_MIMIR.md`

## Rehearsal

Run the ignored `.station-private/pr532/operator.mjs` in one serialized
foreground sequence:

```text
open-proof
moderation-remove
moderation-restore
retract-delete
verify
```

Use the operator for exact API transitions and inspect the real hosted product
as a human at desktop and `390px`. Follow every route, privacy, no-drift,
moderation, retract/delete, and public-safe JSON requirement in the parent PR532
brief. This is the complete PR524B proof, not a smoke test or screenshot-only
pass.

## Required End State

- both tagged personas deleted;
- exact proof sessions deleted;
- counterparty private whenever it exists and absent at closeout;
- no tagged consent, artifact, revision, approval, publication, audit, report,
  or public route residue;
- generated tables restored to their exact baseline;
- configured account, Auth/session, retained PR528, four migration rows,
  unrelated data, and Railway deployment exact;
- one public-safe result with no raw ids, tokens, private body, report notes,
  credentials, SQL, stack, or secret-shaped values.

If any assertion fails, run `cleanup` before stopping and preserve the encrypted
failure/recovery evidence. Commit a public-safe pass/block result and wake
ARGUS explicitly. Do not return to sleep without a committed handoff.

```text
WAKEUP A4:
Codename: ARIADNE
Summary:
- ARGUS accepted PR532A source; MIMIR applied and ledgered migration 088 and
  independently reconciled all four guards, ten cascade edges, 116 data-table
  fingerprints, dependencies, and Railway state.
- A fresh PR532 read-only preflight now has zero unsafe guards, four exact
  migration rows, zero generated rows, and zero product mutations.
Task:
- Run the complete PR532 API plus human-eye hosted rehearsal through cleanup
  using the ignored recoverable operator and the parent PR532 brief.
- Commit a public-safe result and wake ARGUS. On any failure, run cleanup first;
  do not sleep or stop without an explicit committed handoff.
```
