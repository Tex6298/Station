# PR528B - Partner Corpus Content Brief

Owner: MIMIR / A1

Date: 2026-07-16

Status: Product direction for PR528B-DATA1/DATA2

## Intent

The partner corpus should demonstrate Station without pretending to be a real
customer, research programme, or production community. It must read like
deliberate Station editorial/sample material, not test evidence. Public and
private content stay separate even when one dedicated staging owner creates
both.

No visible title, slug, summary, body, persona, thread, Memory, Archive source,
or Continuity record may contain `test`, `proof`, `replay`, `PR`, an agent name,
an implementation ticket, a synthetic identifier, or staging-cleanup prose.
Operational records may carry a private non-visible partner-pass tag.

## Public Chain

Preferred author display name: `Station Guide`.

Space:

```text
Continuity Field Notes
```

Description:

```text
Public notes on how long-running AI companions can remember, change, and stay accountable.
```

Published document kind: `essay` (or the existing equivalent used by the
public Writing taxonomy).

Document title:

```text
What should a companion keep steady?
```

Summary:

```text
A practical boundary between continuity and reinvention.
```

Body:

```text
A useful companion should not remember everything. It should remember what helps a relationship remain legible: commitments, durable preferences, unresolved questions, and the reasons behind important choices.

Continuity is not sameness. A person can change their mind, abandon a plan, or ask for a new tone. Good memory preserves the path without turning an old version of someone into a permanent instruction.

That means every remembered detail needs provenance. Was it stated by the user, agreed over time, inferred from a conversation, or imported from an archive? The answer changes how confidently the companion should use it.

The practical test is simple: keep enough context to continue with care, and leave enough room for the next conversation to matter.
```

Linked Forum thread title:

```text
What belongs in continuity, and what should be allowed to change?
```

Thread body:

```text
Where would you draw the line between useful continuity and an old preference that should be allowed to fade? Share a principle, a failure mode, or a question you would want a companion to ask before treating something as durable.
```

The chain must be visible from Discover without a guessed search, preferably in
`Latest` unless the existing feature/staff-pick contract can be satisfied
truthfully. Do not falsify engagement, votes, staff endorsement, timestamps, or
authorship to force ranking.

## Private Studio Corpus

Create an isolated partner-review owner only if ARGUS accepts the account,
entitlement, retention, and cleanup boundary. The visible owner name remains
`Station Guide`; credentials and internal tags are never public content.

Private persona:

```text
Aster
```

Summary:

```text
A thoughtful companion for long-running creative work.
```

Presence/style direction:

```text
Warm, specific, and direct about tradeoffs. Separate remembered facts from present inference, and leave room for the user to revise an old preference.
```

Continuity brief:

```text
Keep decisions, open questions, and the reasons behind changes visible. Treat old preferences as revisable rather than permanent instructions.
```

Curated Memory items:

| Title | Summary |
| --- | --- |
| Working rhythm | End planning conversations with one clear next action and preserve unresolved questions for the next session. |
| Feedback preference | Name the tradeoff plainly, then offer a recommendation instead of hiding behind a list of possibilities. |

Curated pending Memory Inbox candidate:

| Title | Summary |
| --- | --- |
| Decision log | Keep a short record of decisions and why they changed, but ask before treating a temporary experiment as durable. |

Private Archive source:

```text
Companion design notes
```

Source body:

```text
Continuity should help a conversation resume without forcing it to repeat. Preserve the reason behind a decision when that reason will matter later.

When a preference changes, keep the old context as provenance rather than using it as the current instruction. Confidence should follow the source: user-stated, agreed, inferred, or imported.
```

Continuity record title:

```text
Collaboration brief
```

Record summary:

```text
Aster keeps decisions and open questions legible while treating preferences as revisable.
```

The first successful private conversation should be generated through the
accepted provider rather than seeded as a fake model reply. Its owner prompt
may ask:

```text
I am revising a long-running project. What should we keep steady, and what should we be willing to change?
```

Do not demand exact model wording. Require a non-empty relevant reply, correct
provider trace, private persistence, refresh/readback, return-to-thread, and no
public/search leakage.

## Retention And Cleanup

This is deliberate review content, not a disposable test. If accepted, retain
it only through Marty and his partner's PR528 review. Before creation, record a
private exact cleanup packet for every account/Auth, provider credential,
persona, conversation/message, Memory, candidate, Archive, Continuity, Space,
document, discussion, and ranking/feed artifact introduced.

After partner feedback, MIMIR must choose explicitly to promote/replace the
editorial sample or execute that cleanup packet. Nothing becomes permanent by
silence. No secret, private id, credential, token, row body, or private
timestamp belongs in committed docs.
