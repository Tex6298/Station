# PR525F - Forums Three-Column Honest Composition Closeout

Owner: MIMIR / A1

Date closed: 2026-07-14

Status:

```text
CLOSE_PR525F_FORUMS_THREE_COLUMN_HONEST_COMPOSITION_ACCEPTED
```

## Accepted Result

PR525F is closed as the accepted honest Forums-index translation for the
terminal light-parity sequence.

- `51e0faf4` supplied the exact measured three-column desktop composition,
  feed-first mobile stack, real Tex category/community routes, truthful state
  handling, and focused source assertions.
- ARGUS accepted at `0e090a0c` after one narrow copy patch changed the mixed
  route-group heading from unsupported `Feeds` framing to literal `Navigate`.
- At `1440x900`, the layout is exactly `210px / 720px / 260px` with `18px`
  gaps and the normal feed card is `720px x 128px`.
- At `390x844` and `375x812`, the feeds are exactly `354px` and `339px` at x
  `18`, normal cards are `172px`, context follows the feed, desktop rail is
  absent, and native route navigation opens by keyboard.
- Returned categories remain in response order; all visible commands resolve
  to existing routes; loading, error, empty, and long-content states remain
  truthful; no fabricated sort, vote, activity, provenance, fallback, event,
  posting-mode, or live-Salon claim appears.
- Focused `7/7`, community `48/48`, Studio UI `254/254`, Developer Spaces
  `61/61`, typecheck, lint, scope, secret, and independent rendered checks
  pass.

Sources:

- `docs/roadmap/PR525F_FORUMS_THREE_COLUMN_HONEST_COMPOSITION_MIMIR_RESULT.md`
- `docs/roadmap/PR525F_FORUMS_THREE_COLUMN_HONEST_COMPOSITION_ARGUS_RESULT.md`

## Boundary

PR525F changes only the public `/forums` category index, focused index CSS, and
focused tests. Category/thread/create/recognition/report/moderation/
subcommunity behavior, Studio, companion chat, Developer Spaces, PR526 flows,
API, schema, auth implementation, provider, retrieval, storage, billing,
deployment, packages, and lockfiles remain unchanged.

## Next

PR525G opens the hosted light-parity rehearsal. It must prove the accepted
PR525B-F composition on Railway at desktop, `390px`, and `375px` using bounding
rectangles as well as screenshots, retain the accepted visible-deviation
ledger, and cite PR526A/PR526B rather than claiming guided-flow implementation.
