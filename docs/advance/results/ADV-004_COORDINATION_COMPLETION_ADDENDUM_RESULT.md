# ADV-004 - Coordination Completion Addendum

Date: 2026-06-25

Owner: A5 / KVASIR

Status: complete with A6/A7/A8 input

## Summary

This addendum completes ADV-004 with advance-team review. It supersedes the
earlier ADV-004 coordination note that said SESHAT, JANUS, and CASSANDRA were
not woken.

The split-safe future prep register remains useful, but its active-boundary
framing must now reflect current mainline truth:

- PR319 is the active mainline lane.
- ARIADNE owns the hosted public persona report moderation rehearsal.
- The active mainline issue is stale hosted web deployment/freshness.
- PR318 product surfaces remain inherited avoid surfaces because PR319 rehearses
  the deployed PR318 moderation path.

This addendum does not start any listed prep item. It does not recommend a
mainline PR, touch product code, inspect deploy/config, change acceptance bars,
or wake DAEDALUS, ARGUS, or ARIADNE.

## A6/A7/A8 Contributions

### A6 / SESHAT - Archive And Evidence Review

SESHAT found the register strong as a split-safe evidence artifact, especially
because the candidate packets are discardable and artifact-shaped.

Merge points:

- Update the active boundary from PR318-active to PR319-active while preserving
  PR318 moderation surfaces as inherited avoidances.
- Add evidence hygiene: use source type, date, caveat, and status only; do not
  include raw ids, hosted logs, credentials, prompts, completions, provider
  payloads, private source bodies, or deploy config.
- Keep the MIMIR question explicitly optional and answerable at the next natural
  handoff only.
- Treat demo freshness work as docs/status summarization only, with no hosted
  probing, log inspection, or deploy diagnosis.

Top safe candidates from SESHAT:

- public launch claim checklist;
- demo caveat/freshness checklist, if strictly docs-only.

### A7 / JANUS - Boundary And Permission Review

JANUS found the register generally passes the ADV boundary because it stays in
advance docs, remains advisory/discardable, and keeps MIMIR promotion
authority.

Merge points:

- Add PR319-specific avoidances: hosted rehearsal behavior, deploy freshness
  judgment, hosted logs, deploy/config, credentials, raw ids, and active
  ARIADNE evidence.
- Mark demo caveat/freshness work as conditional: safe after PR319, or safe now
  only as a no-probe template.
- Strengthen all candidate packets with no A1-A4 wakeups, no acceptance-bar
  edits, no product claims, no code/routes/tests, no config values, and MIMIR
  promotion required.

Top safe candidates from JANUS:

- public launch claim checklist;
- config and request inventory for future pilots, if categories-only and no
  requests are executed.

### A8 / CASSANDRA - Failure Forecast Review

CASSANDRA found the biggest risk is stale boundary language. If ADV-004 keeps
talking as if PR318 is still active, future prep could accidentally graze
PR319's hosted freshness, moderation rehearsal, or deploy-evidence authority.

Merge points:

- Add a universal defer trigger: if a packet needs current hosted truth while
  hosted deployment freshness is stale or blocked, stop unless MIMIR explicitly
  scopes it as docs-only.
- Add an explicit discard trigger for any packet that needs deploy/config
  inspection, credentials, logs, raw ids, prompts, completions, provider
  payloads, private source bodies, or product-code reads.
- Add "prepared apart can become bad if..." checks to future packet prompts.
- Treat no next ADV packet as a valid answer.

Top safe candidates from CASSANDRA:

- public launch claim checklist;
- demo caveat/freshness checklist, only as "what not to claim while freshness
  is unresolved" and with no probing or deploy investigation.

## Revised Active Boundary

ADV work must avoid:

- PR319 hosted rehearsal work, stale hosted deploy/freshness diagnosis, active
  ARIADNE evidence, and any browser-hosted proof collection;
- `/forums/moderation?targetType=persona` rehearsal behavior and route fit;
- PR318 moderation pointer/readback product surfaces that PR319 is rehearsing;
- hosted logs, credentials, cookies, tokens, env values, raw ids, provider
  payloads, prompts, completions, private source bodies, SQL dumps, deploy
  config, and secret-shaped values;
- acceptance-bar edits, package-script changes, route changes, migrations,
  product code, product tests, and mainline PR recommendations.

## Universal ADV-004 Guardrails

Any follow-up ADV packet from this register should include these constraints:

- It is docs-only unless MIMIR explicitly promotes it.
- It does not wake A1-A4 unless the packet explicitly permits a MIMIR handoff.
- It does not edit active roadmap PR files, product files, deploy/config files,
  package scripts, migrations, or acceptance bars.
- It records categories, questions, evidence sources, and caveats only.
- It discards or defers if it needs current hosted truth while PR319 freshness
  is unresolved.
- It discards if it needs credentials, hosted logs, raw ids, prompts,
  completions, provider payloads, private source bodies, SQL dumps, deploy
  config, or product-code reads.
- It states how preparing the artifact apart could become harmful or
  misleading.
- It treats "wait" or "open no next ADV packet" as a valid MIMIR answer.

## Safest Candidate Packets For MIMIR To Consider

These are optional ADV candidates only. They are not a mainline recommendation.

### 1. Public Launch Claim Checklist

Why safest:

- It can prevent overclaims without touching product, deploy, config, hosted
  proof, or active PR319 evidence.

Strict scope:

- Create a claims/evidence/caveat checklist using safe docs-only sources.
- Do not edit public copy, marketing surfaces, product routes, acceptance bars,
  or roadmap sequencing.

Prepared apart can become bad if:

- It turns into public positioning, launch approval, or a claim that lacks
  current safe evidence.

### 2. Config And Request Inventory For Future Pilots

Why safest:

- It can remain categories-only and help MIMIR know what Marty might later need
  to decide, without requesting values or inspecting config.

Strict scope:

- List categories, decision owners, forbidden evidence, safe placeholder shape,
  and promotion trigger.
- Do not ask for credentials, env values, account ids, logs, hosted payloads,
  deploy state, or raw ids.

Prepared apart can become bad if:

- It becomes a shadow config request, deploy diagnosis, or assumption that pilot
  work is approved.

### 3. Demo Caveat And Freshness Checklist

Why conditionally safe:

- It can help prevent stale or overbroad demo claims while PR319 hosted
  freshness is unresolved.

Strict scope:

- Safe now only as a no-probe template or "what not to claim while freshness is
  unresolved" checklist.
- It must not run hosted probes, inspect logs, diagnose deploy state, or
  duplicate PR319 rehearsal authority.

Prepared apart can become bad if:

- It becomes shadow PR319 work, a hosted deploy verdict, or a fresh demo proof.

## Polite MIMIR Handoff

Sorry to interrupt the mainline watch. I know PR319/stale web deploy is the
active mainline issue. ADV-004 is now complete with A6/A7/A8 input. This does
not ask you to pause or change mainline. When you reach a natural handoff point,
please choose one listed item for A5-A8 to prepare as a separate ADV packet, or
tell us to wait.

Safest optional ADV candidates:

- public launch claim checklist;
- config and request inventory for future pilots;
- demo caveat/freshness checklist, only as a no-probe template or
  "what not to claim while freshness is unresolved" checklist.
